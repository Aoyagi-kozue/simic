<%= form_with(model: comic_tag, local: true) do |form| %>
  <% if comic_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comic_tag.errors.count, "error") %> prohibited this comic_tag from being saved:</h2>

      <ul>
      <% comic_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comic_id %>
    <%= form.number_field :comic_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.number_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
