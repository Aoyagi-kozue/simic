<%= form_with(model: mylist, local: true) do |form| %>
  <% if mylist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mylist.errors.count, "error") %> prohibited this mylist from being saved:</h2>

      <ul>
      <% mylist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :comic_id %>
    <%= form.number_field :comic_id %>
  </div>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.number_field :author_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
